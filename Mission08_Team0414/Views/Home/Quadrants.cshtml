@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<h2>Put quadrants here</h2>

@model List<SubmittedTask>

<div class="container">
    <div class="row">
        <div class="col-4 border">
            Time Management Matrix
        </div>
        <div class="col-4 border">
            Urgent
        </div>
        <div class="col-4 border">
            Not Urgent
        </div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-4 border">
            Important
        </div>
        <div class="col-4 border">
            @foreach (var task in Model)
            {
                <div>@task.TaskName</div>
                <button class="btn btn-primary" onclick="window.location.href='@Url.Action("Tasks", "Home", new { taskId = task.TaskId })'">Edit</button>
                <button class="btn btn-danger" onclick="window.location.href='@Url.Action("Delete", "Home", new { taskId = task.TaskId })'">Delete</button>
            }
        </div>
        <div class="col-4 border">
            @foreach (var task in Model)
            {
                <div>@task.TaskName</div>
                <button class="btn btn-primary" onclick="window.location.href='@Url.Action("Tasks", "Home", new { taskId = task.TaskId })'">Edit</button>
                <button class="btn btn-danger" onclick="window.location.href='@Url.Action("Delete", "Home", new { taskId = task.TaskId })'">Delete</button>
            }
        </div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-4 border">
            Not Important
        </div>
        <div class="col-4 border">
            @foreach (var task in Model)
            {
                <div>@task.TaskName</div>
                <button class="btn btn-primary" onclick="window.location.href='@Url.Action("Tasks", "Home", new { taskId = task.TaskId })'">Edit</button>
                <button class="btn btn-danger" onclick="window.location.href='@Url.Action("Delete", "Home", new { taskId = task.TaskId })'">Delete</button>
            }
        </div>
        <div class="col-4 border">
            @foreach (var task in Model)
            {
                <div>@task.TaskName</div>
                <button class="btn btn-primary" onclick="window.location.href='@Url.Action("Tasks", "Home", new { taskId = task.TaskId })'">Edit</button>
                <button class="btn btn-danger" onclick="window.location.href='@Url.Action("Delete", "Home", new { taskId = task.TaskId })'">Delete</button>
            }
        </div>
    </div>
</div>



